
<div class="contents row" >
  <% @oranges.each do |orange| %>
      <div class="content_post" style="background-image: url(<%= orange.image %>);">
            <%# <% if user_signed_in? && current_user.id == orange.user_id %>
      <div class="more">
        <span><%= image_tag 'arrow_top.png' %></span>
        <ul class="more_list">
          <li>
            <%= link_to '詳細', "/ways/#{orange.id}", method: :get %>
          </li>
          <% if user_signed_in? && current_user.id == orange.user_id %>
          <li>
            <%= link_to '編集', "/ways/#{orange.id}/edit", method: :get %>
          </li>
          <li>
            <%= link_to '削除', "/ways/#{orange.id}", method: :delete %>
          </li>
          <% end %>
        </ul>
      </div>
        <%= simple_format(orange.text) %>
        <span class="name">
          <a href="/users/<%= orange.user_id %>">
            <span>投稿者</span><%= orange.user.nickname %>
          </a>
        </span>
      </div>
  <% end %>
  <%= paginate(@oranges) %>
</div>